<h1 class="font-semibold text-3xl mb-4"><%= t('banks.new_bank_title') %></h1>

<%= render "form", bank: @bank %>

<div class="mt-4">
  <%= link_to t('banks.back_to_banks'), banks_path, class: "text-blue-500 hover:underline" %>
</div>